overwrite: true
schema: 'http://localhost:4000/graphql'
generates:
  app/schema/graphql.ts:
    plugins:
      - 'typescript'
      - 'typescript-operations'
      - 'fragment-matcher'
      - typescript-resolvers
  ./graphql.schema.json:
    plugins:
      - 'introspection'
