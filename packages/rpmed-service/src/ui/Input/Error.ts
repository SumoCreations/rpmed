import styled from '../styled-components'

export const Error = styled.span`
  display: flex;
  color: ${(p: any) => p.theme.colorErrorText};
  font-size: 12px;
  font-weight: bold;
  padding: 0.5em 0;
`
