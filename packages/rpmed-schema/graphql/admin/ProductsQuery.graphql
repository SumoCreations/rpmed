query Products($search: String) {
  response: products(search: $search) {
    products {
      id
      name
      description
    }
    pageSize
    success
    errors {
      message
      path
    }
  }
}
