mutation destroyProductSymptom($id: String!) {
  response: destroyProductSymptom(id: $id) {
    productSymptom {
      id
      name
      faultCode
      fee
    }
    errors {
      message
      path
    }
  }
}
