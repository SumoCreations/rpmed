query Document($id: ID!) {
  response: document(id: $id) {
    document {
      id
      title
      keywords
      description
      slug
    }
    success
    errors {
      path
      message
    }
  }
}
