import styled from '../styled-components'

export const Light = styled.hr`
  margin: 0.5rem 0;
  border: 0;
  border-top: 0.05rem solid ${p => p.theme.colorContentAreaBorder};
  color: transparent;
`
