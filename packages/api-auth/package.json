{
  "name": "api-auth",
  "version": "1.0.0",
  "types": "./lib/index.d.ts",
  "files": [
    "lib"
  ],
  "main": "lib/index.js",
  "private": true,
  "sideEffects": false,
  "scripts": {
    "build": "rm -rf ./lib && tsc",
    "build-test": "rm -rf ./lib && tsc -m CommonJS",
    "test": "jest"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3"
  },
  "devDependencies": {
    "@types/bcryptjs": "^2.4.2",
    "@types/jsonwebtoken": "^8.3.3",
    "aws-sdk": "^2.606.0",
    "dotenv": "^8.2.0",
    "jest": "26.6.0",
    "jest-raw-loader": "^1.0.1",
    "prettier": "^2.1.2",
    "ts-jest": "^26.4.1",
    "typescript": "^4.0.5"
  }
}
